import React, { Component } from 'react';
import { View, Text } from 'react-native';

class Spot extends Component {

    // TODO: Create Spot Page template that will be used to display all the information that was inputted by user when saving the specific location

    render() {
        return (
            <View>

            </View>
        );
    }
}

export default Spot;